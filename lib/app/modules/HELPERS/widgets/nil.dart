import 'package:flutter/material.dart';

class NotExpensiveWidget extends StatelessWidget {
  const NotExpensiveWidget({Key? key}) : super(key: key);


// like this i found the most unexpensive way to do this
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}