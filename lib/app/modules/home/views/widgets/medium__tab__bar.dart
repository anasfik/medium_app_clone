import 'package:flutter/material.dart';

import '../../constants.dart';

class MediumTabBar extends StatelessWidget {
  const MediumTabBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: tabBarHeight,
      color: Colors.purple,
    );
  }
}
